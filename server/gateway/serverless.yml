# serverless.yml

service: job-server

provider:
  name: aws
  runtime: nodejs10.x
  stage: dev
  region: us-west-2

plugins:
  - serverless-offline

functions:
  app:
    handler: index.handler
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'
